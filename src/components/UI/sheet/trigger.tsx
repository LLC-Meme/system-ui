"use client";

import * as Dialog from "@radix-ui/react-dialog";

/**
 * シートを表示するトリガーを提供するコンポーネント
 * これをクリックすることでシートを表示することができる
 */
const SheetTrigger = Dialog.Trigger;
SheetTrigger.displayName = "SheetTrigger";

export default SheetTrigger;
